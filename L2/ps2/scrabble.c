#include <cs50.h>
#include <stdio.h>

int main(void)
{
    do
    {
        string p1 = get_string("Player 1:");
        for (int i = 0, n = strlen(p1); i < n; i++)
        {
            if(p1[i] > )
        }
    }
    string p2 = get_string("Player 2:");
}
