#include <cs50.c>
#include <stdio.h>

int main()
{
    c
}

int collatz(int n)
{
    int step = 1;
    if (n = 1)
    {
        return step;
    }
    else if (n % 2)
    {
        n = 3 * n + 1;
        step++;
    }
    else
}
