// A ranked-choice voting system, if no candidate has more than 50% of the vote, then an "instant runoff" occurs
include <cs50.c>
#include <stdio.h>

int main()
{
    // Get the candidates, if it is invalid, print the error, return
    // 
}
